/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.chronicle.labs.api.resources.backtests.types;

import com.chronicle.labs.api.core.Nullable;
import com.chronicle.labs.api.core.NullableNonemptyFilter;
import com.chronicle.labs.api.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Double;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = CreateBacktestJobRequestRecipeGradersItem.Builder.class
)
public final class CreateBacktestJobRequestRecipeGradersItem {
  private final Optional<CreateBacktestJobRequestRecipeGradersItemCode> code;

  private final Optional<String> evidence;

  private final String id;

  private final Optional<CreateBacktestJobRequestRecipeGradersItemJudge> judge;

  private final CreateBacktestJobRequestRecipeGradersItemKind kind;

  private final String label;

  private final Optional<Double> passThreshold;

  private final Optional<String> scorerId;

  private final CreateBacktestJobRequestRecipeGradersItemSource source;

  private final CreateBacktestJobRequestRecipeGradersItemWeight weight;

  private final Map<String, Object> additionalProperties;

  private CreateBacktestJobRequestRecipeGradersItem(
      Optional<CreateBacktestJobRequestRecipeGradersItemCode> code, Optional<String> evidence,
      String id, Optional<CreateBacktestJobRequestRecipeGradersItemJudge> judge,
      CreateBacktestJobRequestRecipeGradersItemKind kind, String label,
      Optional<Double> passThreshold, Optional<String> scorerId,
      CreateBacktestJobRequestRecipeGradersItemSource source,
      CreateBacktestJobRequestRecipeGradersItemWeight weight,
      Map<String, Object> additionalProperties) {
    this.code = code;
    this.evidence = evidence;
    this.id = id;
    this.judge = judge;
    this.kind = kind;
    this.label = label;
    this.passThreshold = passThreshold;
    this.scorerId = scorerId;
    this.source = source;
    this.weight = weight;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Code handler snapshot — required when <code>kind</code> is <code>code</code>.
   */
  @JsonIgnore
  public Optional<CreateBacktestJobRequestRecipeGradersItemCode> getCode() {
    if (code == null) {
      return Optional.empty();
    }
    return code;
  }

  /**
   * @return Optional human-readable explanation of why this grader was proposed. For rubric graders this doubles as the judge prompt; library scorers inline their prompt here at launch so the recipe stays self-describing.
   */
  @JsonIgnore
  public Optional<String> getEvidence() {
    if (evidence == null) {
      return Optional.empty();
    }
    return evidence;
  }

  @JsonProperty("id")
  public String getId() {
    return id;
  }

  /**
   * @return LLM-judge configuration snapshot (rubric graders). <code>None</code> falls back to the default free-score judge behavior.
   */
  @JsonIgnore
  public Optional<CreateBacktestJobRequestRecipeGradersItemJudge> getJudge() {
    if (judge == null) {
      return Optional.empty();
    }
    return judge;
  }

  @JsonProperty("kind")
  public CreateBacktestJobRequestRecipeGradersItemKind getKind() {
    return kind;
  }

  @JsonProperty("label")
  public String getLabel() {
    return label;
  }

  /**
   * @return Scores at or above this value render as passing in results.
   */
  @JsonIgnore
  public Optional<Double> getPassThreshold() {
    if (passThreshold == null) {
      return Optional.empty();
    }
    return passThreshold;
  }

  /**
   * @return Provenance: the library <code>Scorer.id</code> this grader was created from (when <code>source</code> is <code>library</code>). The prompt is snapshotted into <code>evidence</code> at launch, so editing the scorer later never mutates a past run.
   */
  @JsonIgnore
  public Optional<String> getScorerId() {
    if (scorerId == null) {
      return Optional.empty();
    }
    return scorerId;
  }

  /**
   * @return Grader source — where this grader came from when it was added to the recipe. Determines the chip copy (&quot;proposed&quot; vs &quot;library&quot; vs &quot;custom&quot; vs &quot;dataset&quot;).
   */
  @JsonProperty("source")
  public CreateBacktestJobRequestRecipeGradersItemSource getSource() {
    return source;
  }

  /**
   * @return Grader weight bucket — <code>low | med | high</code> matches the segmented control in the GraderBuilder tray.
   */
  @JsonProperty("weight")
  public CreateBacktestJobRequestRecipeGradersItemWeight getWeight() {
    return weight;
  }

  @JsonInclude(
      value = JsonInclude.Include.CUSTOM,
      valueFilter = NullableNonemptyFilter.class
  )
  @JsonProperty("code")
  private Optional<CreateBacktestJobRequestRecipeGradersItemCode> _getCode() {
    return code;
  }

  @JsonInclude(
      value = JsonInclude.Include.CUSTOM,
      valueFilter = NullableNonemptyFilter.class
  )
  @JsonProperty("evidence")
  private Optional<String> _getEvidence() {
    return evidence;
  }

  @JsonInclude(
      value = JsonInclude.Include.CUSTOM,
      valueFilter = NullableNonemptyFilter.class
  )
  @JsonProperty("judge")
  private Optional<CreateBacktestJobRequestRecipeGradersItemJudge> _getJudge() {
    return judge;
  }

  @JsonInclude(
      value = JsonInclude.Include.CUSTOM,
      valueFilter = NullableNonemptyFilter.class
  )
  @JsonProperty("passThreshold")
  private Optional<Double> _getPassThreshold() {
    return passThreshold;
  }

  @JsonInclude(
      value = JsonInclude.Include.CUSTOM,
      valueFilter = NullableNonemptyFilter.class
  )
  @JsonProperty("scorerId")
  private Optional<String> _getScorerId() {
    return scorerId;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof CreateBacktestJobRequestRecipeGradersItem && equalTo((CreateBacktestJobRequestRecipeGradersItem) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(CreateBacktestJobRequestRecipeGradersItem other) {
    return code.equals(other.code) && evidence.equals(other.evidence) && id.equals(other.id) && judge.equals(other.judge) && kind.equals(other.kind) && label.equals(other.label) && passThreshold.equals(other.passThreshold) && scorerId.equals(other.scorerId) && source.equals(other.source) && weight.equals(other.weight);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.code, this.evidence, this.id, this.judge, this.kind, this.label, this.passThreshold, this.scorerId, this.source, this.weight);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static IdStage builder() {
    return new Builder();
  }

  public interface IdStage {
    KindStage id(@NotNull String id);

    Builder from(CreateBacktestJobRequestRecipeGradersItem other);
  }

  public interface KindStage {
    LabelStage kind(@NotNull CreateBacktestJobRequestRecipeGradersItemKind kind);
  }

  public interface LabelStage {
    SourceStage label(@NotNull String label);
  }

  public interface SourceStage {
    /**
     * <p>Grader source — where this grader came from when it was added to the recipe. Determines the chip copy (&quot;proposed&quot; vs &quot;library&quot; vs &quot;custom&quot; vs &quot;dataset&quot;).</p>
     */
    WeightStage source(@NotNull CreateBacktestJobRequestRecipeGradersItemSource source);
  }

  public interface WeightStage {
    /**
     * <p>Grader weight bucket — <code>low | med | high</code> matches the segmented control in the GraderBuilder tray.</p>
     */
    _FinalStage weight(@NotNull CreateBacktestJobRequestRecipeGradersItemWeight weight);
  }

  public interface _FinalStage {
    CreateBacktestJobRequestRecipeGradersItem build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>Code handler snapshot — required when <code>kind</code> is <code>code</code>.</p>
     */
    _FinalStage code(Optional<CreateBacktestJobRequestRecipeGradersItemCode> code);

    _FinalStage code(CreateBacktestJobRequestRecipeGradersItemCode code);

    _FinalStage code(Nullable<CreateBacktestJobRequestRecipeGradersItemCode> code);

    /**
     * <p>Optional human-readable explanation of why this grader was proposed. For rubric graders this doubles as the judge prompt; library scorers inline their prompt here at launch so the recipe stays self-describing.</p>
     */
    _FinalStage evidence(Optional<String> evidence);

    _FinalStage evidence(String evidence);

    _FinalStage evidence(Nullable<String> evidence);

    /**
     * <p>LLM-judge configuration snapshot (rubric graders). <code>None</code> falls back to the default free-score judge behavior.</p>
     */
    _FinalStage judge(Optional<CreateBacktestJobRequestRecipeGradersItemJudge> judge);

    _FinalStage judge(CreateBacktestJobRequestRecipeGradersItemJudge judge);

    _FinalStage judge(Nullable<CreateBacktestJobRequestRecipeGradersItemJudge> judge);

    /**
     * <p>Scores at or above this value render as passing in results.</p>
     */
    _FinalStage passThreshold(Optional<Double> passThreshold);

    _FinalStage passThreshold(Double passThreshold);

    _FinalStage passThreshold(Nullable<Double> passThreshold);

    /**
     * <p>Provenance: the library <code>Scorer.id</code> this grader was created from (when <code>source</code> is <code>library</code>). The prompt is snapshotted into <code>evidence</code> at launch, so editing the scorer later never mutates a past run.</p>
     */
    _FinalStage scorerId(Optional<String> scorerId);

    _FinalStage scorerId(String scorerId);

    _FinalStage scorerId(Nullable<String> scorerId);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements IdStage, KindStage, LabelStage, SourceStage, WeightStage, _FinalStage {
    private String id;

    private CreateBacktestJobRequestRecipeGradersItemKind kind;

    private String label;

    private CreateBacktestJobRequestRecipeGradersItemSource source;

    private CreateBacktestJobRequestRecipeGradersItemWeight weight;

    private Optional<String> scorerId = Optional.empty();

    private Optional<Double> passThreshold = Optional.empty();

    private Optional<CreateBacktestJobRequestRecipeGradersItemJudge> judge = Optional.empty();

    private Optional<String> evidence = Optional.empty();

    private Optional<CreateBacktestJobRequestRecipeGradersItemCode> code = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(CreateBacktestJobRequestRecipeGradersItem other) {
      code(other.getCode());
      evidence(other.getEvidence());
      id(other.getId());
      judge(other.getJudge());
      kind(other.getKind());
      label(other.getLabel());
      passThreshold(other.getPassThreshold());
      scorerId(other.getScorerId());
      source(other.getSource());
      weight(other.getWeight());
      return this;
    }

    @java.lang.Override
    @JsonSetter("id")
    public KindStage id(@NotNull String id) {
      this.id = Objects.requireNonNull(id, "id must not be null");
      return this;
    }

    @java.lang.Override
    @JsonSetter("kind")
    public LabelStage kind(@NotNull CreateBacktestJobRequestRecipeGradersItemKind kind) {
      this.kind = Objects.requireNonNull(kind, "kind must not be null");
      return this;
    }

    @java.lang.Override
    @JsonSetter("label")
    public SourceStage label(@NotNull String label) {
      this.label = Objects.requireNonNull(label, "label must not be null");
      return this;
    }

    /**
     * <p>Grader source — where this grader came from when it was added to the recipe. Determines the chip copy (&quot;proposed&quot; vs &quot;library&quot; vs &quot;custom&quot; vs &quot;dataset&quot;).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("source")
    public WeightStage source(@NotNull CreateBacktestJobRequestRecipeGradersItemSource source) {
      this.source = Objects.requireNonNull(source, "source must not be null");
      return this;
    }

    /**
     * <p>Grader weight bucket — <code>low | med | high</code> matches the segmented control in the GraderBuilder tray.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("weight")
    public _FinalStage weight(@NotNull CreateBacktestJobRequestRecipeGradersItemWeight weight) {
      this.weight = Objects.requireNonNull(weight, "weight must not be null");
      return this;
    }

    /**
     * <p>Provenance: the library <code>Scorer.id</code> this grader was created from (when <code>source</code> is <code>library</code>). The prompt is snapshotted into <code>evidence</code> at launch, so editing the scorer later never mutates a past run.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage scorerId(Nullable<String> scorerId) {
      if (scorerId.isNull()) {
        this.scorerId = null;
      }
      else if (scorerId.isEmpty()) {
        this.scorerId = Optional.empty();
      }
      else {
        this.scorerId = Optional.of(scorerId.get());
      }
      return this;
    }

    /**
     * <p>Provenance: the library <code>Scorer.id</code> this grader was created from (when <code>source</code> is <code>library</code>). The prompt is snapshotted into <code>evidence</code> at launch, so editing the scorer later never mutates a past run.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage scorerId(String scorerId) {
      this.scorerId = Optional.ofNullable(scorerId);
      return this;
    }

    /**
     * <p>Provenance: the library <code>Scorer.id</code> this grader was created from (when <code>source</code> is <code>library</code>). The prompt is snapshotted into <code>evidence</code> at launch, so editing the scorer later never mutates a past run.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "scorerId",
        nulls = Nulls.SKIP
    )
    public _FinalStage scorerId(Optional<String> scorerId) {
      this.scorerId = scorerId;
      return this;
    }

    /**
     * <p>Scores at or above this value render as passing in results.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage passThreshold(Nullable<Double> passThreshold) {
      if (passThreshold.isNull()) {
        this.passThreshold = null;
      }
      else if (passThreshold.isEmpty()) {
        this.passThreshold = Optional.empty();
      }
      else {
        this.passThreshold = Optional.of(passThreshold.get());
      }
      return this;
    }

    /**
     * <p>Scores at or above this value render as passing in results.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage passThreshold(Double passThreshold) {
      this.passThreshold = Optional.ofNullable(passThreshold);
      return this;
    }

    /**
     * <p>Scores at or above this value render as passing in results.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "passThreshold",
        nulls = Nulls.SKIP
    )
    public _FinalStage passThreshold(Optional<Double> passThreshold) {
      this.passThreshold = passThreshold;
      return this;
    }

    /**
     * <p>LLM-judge configuration snapshot (rubric graders). <code>None</code> falls back to the default free-score judge behavior.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage judge(Nullable<CreateBacktestJobRequestRecipeGradersItemJudge> judge) {
      if (judge.isNull()) {
        this.judge = null;
      }
      else if (judge.isEmpty()) {
        this.judge = Optional.empty();
      }
      else {
        this.judge = Optional.of(judge.get());
      }
      return this;
    }

    /**
     * <p>LLM-judge configuration snapshot (rubric graders). <code>None</code> falls back to the default free-score judge behavior.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage judge(CreateBacktestJobRequestRecipeGradersItemJudge judge) {
      this.judge = Optional.ofNullable(judge);
      return this;
    }

    /**
     * <p>LLM-judge configuration snapshot (rubric graders). <code>None</code> falls back to the default free-score judge behavior.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "judge",
        nulls = Nulls.SKIP
    )
    public _FinalStage judge(Optional<CreateBacktestJobRequestRecipeGradersItemJudge> judge) {
      this.judge = judge;
      return this;
    }

    /**
     * <p>Optional human-readable explanation of why this grader was proposed. For rubric graders this doubles as the judge prompt; library scorers inline their prompt here at launch so the recipe stays self-describing.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage evidence(Nullable<String> evidence) {
      if (evidence.isNull()) {
        this.evidence = null;
      }
      else if (evidence.isEmpty()) {
        this.evidence = Optional.empty();
      }
      else {
        this.evidence = Optional.of(evidence.get());
      }
      return this;
    }

    /**
     * <p>Optional human-readable explanation of why this grader was proposed. For rubric graders this doubles as the judge prompt; library scorers inline their prompt here at launch so the recipe stays self-describing.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage evidence(String evidence) {
      this.evidence = Optional.ofNullable(evidence);
      return this;
    }

    /**
     * <p>Optional human-readable explanation of why this grader was proposed. For rubric graders this doubles as the judge prompt; library scorers inline their prompt here at launch so the recipe stays self-describing.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "evidence",
        nulls = Nulls.SKIP
    )
    public _FinalStage evidence(Optional<String> evidence) {
      this.evidence = evidence;
      return this;
    }

    /**
     * <p>Code handler snapshot — required when <code>kind</code> is <code>code</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage code(Nullable<CreateBacktestJobRequestRecipeGradersItemCode> code) {
      if (code.isNull()) {
        this.code = null;
      }
      else if (code.isEmpty()) {
        this.code = Optional.empty();
      }
      else {
        this.code = Optional.of(code.get());
      }
      return this;
    }

    /**
     * <p>Code handler snapshot — required when <code>kind</code> is <code>code</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage code(CreateBacktestJobRequestRecipeGradersItemCode code) {
      this.code = Optional.ofNullable(code);
      return this;
    }

    /**
     * <p>Code handler snapshot — required when <code>kind</code> is <code>code</code>.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "code",
        nulls = Nulls.SKIP
    )
    public _FinalStage code(Optional<CreateBacktestJobRequestRecipeGradersItemCode> code) {
      this.code = code;
      return this;
    }

    @java.lang.Override
    public CreateBacktestJobRequestRecipeGradersItem build() {
      return new CreateBacktestJobRequestRecipeGradersItem(code, evidence, id, judge, kind, label, passThreshold, scorerId, source, weight, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
