/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.chronicle.labs.api.resources.environments;

import com.chronicle.labs.api.core.ChronicleLabsApiApiException;
import com.chronicle.labs.api.core.ChronicleLabsApiException;
import com.chronicle.labs.api.core.ChronicleLabsApiHttpResponse;
import com.chronicle.labs.api.core.ClientOptions;
import com.chronicle.labs.api.core.MediaTypes;
import com.chronicle.labs.api.core.ObjectMappers;
import com.chronicle.labs.api.core.RequestOptions;
import com.chronicle.labs.api.core.RetryInterceptor;
import com.chronicle.labs.api.errors.BadRequestError;
import com.chronicle.labs.api.errors.NotFoundError;
import com.chronicle.labs.api.errors.ServiceUnavailableError;
import com.chronicle.labs.api.errors.UnauthorizedError;
import com.chronicle.labs.api.resources.environments.requests.CompileEnvironmentRequest;
import com.chronicle.labs.api.resources.environments.requests.CreateEnvironmentRequest;
import com.chronicle.labs.api.resources.environments.requests.CreateEnvironmentVersionRequest;
import com.chronicle.labs.api.resources.environments.requests.GetEnvironmentRequest;
import com.chronicle.labs.api.resources.environments.requests.GetEnvironmentVersionRequest;
import com.chronicle.labs.api.resources.environments.requests.ListEnvironmentVersionsRequest;
import com.chronicle.labs.api.types.CompileEnvironmentResponse;
import com.chronicle.labs.api.types.EnvironmentResponse;
import com.chronicle.labs.api.types.EnvironmentVersionRecord;
import com.chronicle.labs.api.types.EnvironmentVersionResponse;
import com.chronicle.labs.api.types.ErrorResponse;
import com.chronicle.labs.api.types.ListEnvironmentsResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import java.io.IOException;
import java.util.List;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawEnvironmentsClient {
    protected final ClientOptions clientOptions;

    public RawEnvironmentsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public ChronicleLabsApiHttpResponse<ListEnvironmentsResponse> listEnvironments() {
        return listEnvironments(null);
    }

    public ChronicleLabsApiHttpResponse<ListEnvironmentsResponse> listEnvironments(RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/environments");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ChronicleLabsApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListEnvironmentsResponse.class),
                        response);
            }
            try {
                if (response.code() == 401) {
                    throw new UnauthorizedError(
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ChronicleLabsApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ChronicleLabsApiException("Network error executing HTTP request", e);
        }
    }

    public ChronicleLabsApiHttpResponse<EnvironmentResponse> createEnvironment(CreateEnvironmentRequest request) {
        return createEnvironment(request, null);
    }

    public ChronicleLabsApiHttpResponse<EnvironmentResponse> createEnvironment(
            CreateEnvironmentRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/environments");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ChronicleLabsApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EnvironmentResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ChronicleLabsApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ChronicleLabsApiException("Network error executing HTTP request", e);
        }
    }

    public ChronicleLabsApiHttpResponse<EnvironmentResponse> getEnvironment(String environmentId) {
        return getEnvironment(environmentId, GetEnvironmentRequest.builder().build());
    }

    public ChronicleLabsApiHttpResponse<EnvironmentResponse> getEnvironment(
            String environmentId, RequestOptions requestOptions) {
        return getEnvironment(environmentId, GetEnvironmentRequest.builder().build(), requestOptions);
    }

    public ChronicleLabsApiHttpResponse<EnvironmentResponse> getEnvironment(
            String environmentId, GetEnvironmentRequest request) {
        return getEnvironment(environmentId, request, null);
    }

    public ChronicleLabsApiHttpResponse<EnvironmentResponse> getEnvironment(
            String environmentId, GetEnvironmentRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/environments")
                .addPathSegment(environmentId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ChronicleLabsApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EnvironmentResponse.class), response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ChronicleLabsApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ChronicleLabsApiException("Network error executing HTTP request", e);
        }
    }

    public ChronicleLabsApiHttpResponse<List<EnvironmentVersionRecord>> listEnvironmentVersions(String environmentId) {
        return listEnvironmentVersions(
                environmentId, ListEnvironmentVersionsRequest.builder().build());
    }

    public ChronicleLabsApiHttpResponse<List<EnvironmentVersionRecord>> listEnvironmentVersions(
            String environmentId, RequestOptions requestOptions) {
        return listEnvironmentVersions(
                environmentId, ListEnvironmentVersionsRequest.builder().build(), requestOptions);
    }

    public ChronicleLabsApiHttpResponse<List<EnvironmentVersionRecord>> listEnvironmentVersions(
            String environmentId, ListEnvironmentVersionsRequest request) {
        return listEnvironmentVersions(environmentId, request, null);
    }

    public ChronicleLabsApiHttpResponse<List<EnvironmentVersionRecord>> listEnvironmentVersions(
            String environmentId, ListEnvironmentVersionsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/environments")
                .addPathSegment(environmentId)
                .addPathSegments("versions");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ChronicleLabsApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, new TypeReference<List<EnvironmentVersionRecord>>() {}),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ChronicleLabsApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ChronicleLabsApiException("Network error executing HTTP request", e);
        }
    }

    public ChronicleLabsApiHttpResponse<EnvironmentVersionResponse> createEnvironmentVersion(
            String environmentId, CreateEnvironmentVersionRequest request) {
        return createEnvironmentVersion(environmentId, request, null);
    }

    public ChronicleLabsApiHttpResponse<EnvironmentVersionResponse> createEnvironmentVersion(
            String environmentId, CreateEnvironmentVersionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/environments")
                .addPathSegment(environmentId)
                .addPathSegments("versions");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ChronicleLabsApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EnvironmentVersionResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ChronicleLabsApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ChronicleLabsApiException("Network error executing HTTP request", e);
        }
    }

    public ChronicleLabsApiHttpResponse<EnvironmentVersionResponse> getEnvironmentVersion(
            String environmentId, String versionSelector) {
        return getEnvironmentVersion(
                environmentId,
                versionSelector,
                GetEnvironmentVersionRequest.builder().build());
    }

    public ChronicleLabsApiHttpResponse<EnvironmentVersionResponse> getEnvironmentVersion(
            String environmentId, String versionSelector, RequestOptions requestOptions) {
        return getEnvironmentVersion(
                environmentId,
                versionSelector,
                GetEnvironmentVersionRequest.builder().build(),
                requestOptions);
    }

    public ChronicleLabsApiHttpResponse<EnvironmentVersionResponse> getEnvironmentVersion(
            String environmentId, String versionSelector, GetEnvironmentVersionRequest request) {
        return getEnvironmentVersion(environmentId, versionSelector, request, null);
    }

    public ChronicleLabsApiHttpResponse<EnvironmentVersionResponse> getEnvironmentVersion(
            String environmentId,
            String versionSelector,
            GetEnvironmentVersionRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/environments")
                .addPathSegment(environmentId)
                .addPathSegments("versions")
                .addPathSegment(versionSelector);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ChronicleLabsApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EnvironmentVersionResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ChronicleLabsApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ChronicleLabsApiException("Network error executing HTTP request", e);
        }
    }

    public ChronicleLabsApiHttpResponse<CompileEnvironmentResponse> compileEnvironmentVersion(
            String environmentId, String versionSelector, CompileEnvironmentRequest request) {
        return compileEnvironmentVersion(environmentId, versionSelector, request, null);
    }

    public ChronicleLabsApiHttpResponse<CompileEnvironmentResponse> compileEnvironmentVersion(
            String environmentId,
            String versionSelector,
            CompileEnvironmentRequest request,
            RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/environments")
                .addPathSegment(environmentId)
                .addPathSegments("versions")
                .addPathSegment(versionSelector)
                .addPathSegments("compile");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new ChronicleLabsApiHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CompileEnvironmentResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ChronicleLabsApiApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new ChronicleLabsApiException("Network error executing HTTP request", e);
        }
    }
}
