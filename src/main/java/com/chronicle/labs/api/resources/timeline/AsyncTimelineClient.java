/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.chronicle.labs.api.resources.timeline;

import com.chronicle.labs.api.core.ClientOptions;
import com.chronicle.labs.api.core.RequestOptions;
import com.chronicle.labs.api.resources.timeline.requests.GetTimelineRequest;
import com.chronicle.labs.api.types.EventPage;
import java.util.concurrent.CompletableFuture;

public class AsyncTimelineClient {
    protected final ClientOptions clientOptions;

    private final AsyncRawTimelineClient rawClient;

    public AsyncTimelineClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new AsyncRawTimelineClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public AsyncRawTimelineClient withRawResponse() {
        return this.rawClient;
    }

    /**
     * Requires scope events:read or events:write. Cursor paginated, newest first.
     * <p>Pass <code>cursor</code> from <code>next_cursor</code> to read the following page, and stop when <code>has_more</code> is false. The cursor is opaque: it is a keyset over <code>(event_time, event_id)</code>, it is exclusive so a row cannot repeat across pages, and its encoding may change without notice. Do not parse or construct one.</p>
     * <p><code>include_linked=true</code> selects a different read that also returns causally linked events. That read is not paginated: it returns one page with <code>has_more</code> false, and it cannot be combined with <code>limit</code> or <code>cursor</code>. <code>since</code> is only available on that read, because the paginated read has no time filter.</p>
     */
    public CompletableFuture<EventPage> getTimeline(String entityType, String entityId) {
        return this.rawClient.getTimeline(entityType, entityId).thenApply(response -> response.body());
    }

    /**
     * Requires scope events:read or events:write. Cursor paginated, newest first.
     * <p>Pass <code>cursor</code> from <code>next_cursor</code> to read the following page, and stop when <code>has_more</code> is false. The cursor is opaque: it is a keyset over <code>(event_time, event_id)</code>, it is exclusive so a row cannot repeat across pages, and its encoding may change without notice. Do not parse or construct one.</p>
     * <p><code>include_linked=true</code> selects a different read that also returns causally linked events. That read is not paginated: it returns one page with <code>has_more</code> false, and it cannot be combined with <code>limit</code> or <code>cursor</code>. <code>since</code> is only available on that read, because the paginated read has no time filter.</p>
     */
    public CompletableFuture<EventPage> getTimeline(String entityType, String entityId, RequestOptions requestOptions) {
        return this.rawClient.getTimeline(entityType, entityId, requestOptions).thenApply(response -> response.body());
    }

    /**
     * Requires scope events:read or events:write. Cursor paginated, newest first.
     * <p>Pass <code>cursor</code> from <code>next_cursor</code> to read the following page, and stop when <code>has_more</code> is false. The cursor is opaque: it is a keyset over <code>(event_time, event_id)</code>, it is exclusive so a row cannot repeat across pages, and its encoding may change without notice. Do not parse or construct one.</p>
     * <p><code>include_linked=true</code> selects a different read that also returns causally linked events. That read is not paginated: it returns one page with <code>has_more</code> false, and it cannot be combined with <code>limit</code> or <code>cursor</code>. <code>since</code> is only available on that read, because the paginated read has no time filter.</p>
     */
    public CompletableFuture<EventPage> getTimeline(String entityType, String entityId, GetTimelineRequest request) {
        return this.rawClient.getTimeline(entityType, entityId, request).thenApply(response -> response.body());
    }

    /**
     * Requires scope events:read or events:write. Cursor paginated, newest first.
     * <p>Pass <code>cursor</code> from <code>next_cursor</code> to read the following page, and stop when <code>has_more</code> is false. The cursor is opaque: it is a keyset over <code>(event_time, event_id)</code>, it is exclusive so a row cannot repeat across pages, and its encoding may change without notice. Do not parse or construct one.</p>
     * <p><code>include_linked=true</code> selects a different read that also returns causally linked events. That read is not paginated: it returns one page with <code>has_more</code> false, and it cannot be combined with <code>limit</code> or <code>cursor</code>. <code>since</code> is only available on that read, because the paginated read has no time filter.</p>
     */
    public CompletableFuture<EventPage> getTimeline(
            String entityType, String entityId, GetTimelineRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .getTimeline(entityType, entityId, request, requestOptions)
                .thenApply(response -> response.body());
    }
}
