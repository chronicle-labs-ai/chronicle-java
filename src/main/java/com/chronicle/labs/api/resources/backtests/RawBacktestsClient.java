/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.chronicle.labs.api.resources.backtests;

import com.chronicle.labs.api.core.ChronicleLabsApiApiException;
import com.chronicle.labs.api.core.ChronicleLabsApiException;
import com.chronicle.labs.api.core.ChronicleLabsApiHttpResponse;
import com.chronicle.labs.api.core.ClientOptions;
import com.chronicle.labs.api.core.MediaTypes;
import com.chronicle.labs.api.core.ObjectMappers;
import com.chronicle.labs.api.core.QueryStringMapper;
import com.chronicle.labs.api.core.RequestOptions;
import com.chronicle.labs.api.core.ResponseBodyReader;
import com.chronicle.labs.api.core.RetryInterceptor;
import com.chronicle.labs.api.core.Stream;
import com.chronicle.labs.api.errors.BadRequestError;
import com.chronicle.labs.api.errors.ConflictError;
import com.chronicle.labs.api.errors.NotFoundError;
import com.chronicle.labs.api.errors.ServiceUnavailableError;
import com.chronicle.labs.api.errors.UnauthorizedError;
import com.chronicle.labs.api.resources.backtests.requests.CancelBacktestJobRequest;
import com.chronicle.labs.api.resources.backtests.requests.CreateBacktestJobRequest;
import com.chronicle.labs.api.resources.backtests.requests.GetBacktestJobRequest;
import com.chronicle.labs.api.resources.backtests.requests.GetBacktestTrialRequest;
import com.chronicle.labs.api.resources.backtests.requests.ListBacktestJobTrialsRequest;
import com.chronicle.labs.api.resources.backtests.requests.ListBacktestJobsRequest;
import com.chronicle.labs.api.resources.backtests.requests.StreamBacktestJobEventsRequest;
import com.chronicle.labs.api.types.BacktestJobDetailResponse;
import com.chronicle.labs.api.types.BacktestTrialDetailResponse;
import com.chronicle.labs.api.types.BacktestsAvailability;
import com.chronicle.labs.api.types.CancelBacktestJobResponse;
import com.chronicle.labs.api.types.CreateBacktestJobResponse;
import com.chronicle.labs.api.types.ErrorResponse;
import com.chronicle.labs.api.types.ListBacktestJobTrialsResponse;
import com.chronicle.labs.api.types.ListBacktestJobsResponse;
import com.chronicle.labs.api.types.TrialEvent;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Iterable;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.concurrent.TimeUnit;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawBacktestsClient {
  protected final ClientOptions clientOptions;

  public RawBacktestsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  public ChronicleLabsApiHttpResponse<BacktestsAvailability> getBacktestsAvailability() {
    return getBacktestsAvailability(null);
  }

  public ChronicleLabsApiHttpResponse<BacktestsAvailability> getBacktestsAvailability(
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v1/backtests/availability");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new ChronicleLabsApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BacktestsAvailability.class), response);
        }
        try {
          switch (response.code()) {
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
            case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new ChronicleLabsApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
      }
      catch (IOException e) {
        throw new ChronicleLabsApiException("Network error executing HTTP request", e);
      }
    }

    public ChronicleLabsApiHttpResponse<ListBacktestJobsResponse> listBacktestJobs() {
      return listBacktestJobs(ListBacktestJobsRequest.builder().build());
    }

    public ChronicleLabsApiHttpResponse<ListBacktestJobsResponse> listBacktestJobs(
        RequestOptions requestOptions) {
      return listBacktestJobs(ListBacktestJobsRequest.builder().build(),requestOptions);
    }

    public ChronicleLabsApiHttpResponse<ListBacktestJobsResponse> listBacktestJobs(
        ListBacktestJobsRequest request) {
      return listBacktestJobs(request,null);
    }

    public ChronicleLabsApiHttpResponse<ListBacktestJobsResponse> listBacktestJobs(
        ListBacktestJobsRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v1/backtests/jobs");if (request.getMode().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "mode", request.getMode().get(), false);
        }
        if (request.getStatus().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "status", request.getStatus().get(), false);
        }
        if (request.getLimit().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getOffset().isPresent()) {
          QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
        }
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new ChronicleLabsApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListBacktestJobsResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new ChronicleLabsApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
        }
        catch (IOException e) {
          throw new ChronicleLabsApiException("Network error executing HTTP request", e);
        }
      }

      /**
       * Returns 202 after the durable job and its trials have been admitted.
       */
      public ChronicleLabsApiHttpResponse<CreateBacktestJobResponse> createBacktestJob(
          CreateBacktestJobRequest request) {
        return createBacktestJob(request,null);
      }

      /**
       * Returns 202 after the durable job and its trials have been admitted.
       */
      public ChronicleLabsApiHttpResponse<CreateBacktestJobResponse> createBacktestJob(
          CreateBacktestJobRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v1/backtests/jobs");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(Exception e) {
            throw new RuntimeException(e);
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json");
          if (request.getIdempotencyKey().isPresent()) {
            _requestBuilder.addHeader("Idempotency-Key", request.getIdempotencyKey().get());
          }
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new ChronicleLabsApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateBacktestJobResponse.class), response);
            }
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new ChronicleLabsApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (JsonProcessingException e) {
            throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
          }
          catch (IOException e) {
            throw new ChronicleLabsApiException("Network error executing HTTP request", e);
          }
        }

        public ChronicleLabsApiHttpResponse<BacktestJobDetailResponse> getBacktestJob(
            String jobId) {
          return getBacktestJob(jobId,GetBacktestJobRequest.builder().build());
        }

        public ChronicleLabsApiHttpResponse<BacktestJobDetailResponse> getBacktestJob(String jobId,
            RequestOptions requestOptions) {
          return getBacktestJob(jobId,GetBacktestJobRequest.builder().build(),requestOptions);
        }

        public ChronicleLabsApiHttpResponse<BacktestJobDetailResponse> getBacktestJob(String jobId,
            GetBacktestJobRequest request) {
          return getBacktestJob(jobId,request,null);
        }

        public ChronicleLabsApiHttpResponse<BacktestJobDetailResponse> getBacktestJob(String jobId,
            GetBacktestJobRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v1/backtests/jobs")
            .addPathSegment(jobId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("GET", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new ChronicleLabsApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BacktestJobDetailResponse.class), response);
              }
              try {
                switch (response.code()) {
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new ChronicleLabsApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (JsonProcessingException e) {
              throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
            }
            catch (IOException e) {
              throw new ChronicleLabsApiException("Network error executing HTTP request", e);
            }
          }

          public ChronicleLabsApiHttpResponse<ListBacktestJobTrialsResponse> listBacktestJobTrials(
              String jobId) {
            return listBacktestJobTrials(jobId,ListBacktestJobTrialsRequest.builder().build());
          }

          public ChronicleLabsApiHttpResponse<ListBacktestJobTrialsResponse> listBacktestJobTrials(
              String jobId, RequestOptions requestOptions) {
            return listBacktestJobTrials(jobId,ListBacktestJobTrialsRequest.builder().build(),requestOptions);
          }

          public ChronicleLabsApiHttpResponse<ListBacktestJobTrialsResponse> listBacktestJobTrials(
              String jobId, ListBacktestJobTrialsRequest request) {
            return listBacktestJobTrials(jobId,request,null);
          }

          public ChronicleLabsApiHttpResponse<ListBacktestJobTrialsResponse> listBacktestJobTrials(
              String jobId, ListBacktestJobTrialsRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("v1/backtests/jobs")
              .addPathSegment(jobId)
              .addPathSegments("trials");if (request.getLimit().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
              }
              if (request.getOffset().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
              }
              if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new ChronicleLabsApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListBacktestJobTrialsResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new ChronicleLabsApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (JsonProcessingException e) {
                throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
              }
              catch (IOException e) {
                throw new ChronicleLabsApiException("Network error executing HTTP request", e);
              }
            }

            public ChronicleLabsApiHttpResponse<BacktestTrialDetailResponse> getBacktestTrial(
                String jobId, String trialId) {
              return getBacktestTrial(jobId,trialId,GetBacktestTrialRequest.builder().build());
            }

            public ChronicleLabsApiHttpResponse<BacktestTrialDetailResponse> getBacktestTrial(
                String jobId, String trialId, RequestOptions requestOptions) {
              return getBacktestTrial(jobId,trialId,GetBacktestTrialRequest.builder().build(),requestOptions);
            }

            public ChronicleLabsApiHttpResponse<BacktestTrialDetailResponse> getBacktestTrial(
                String jobId, String trialId, GetBacktestTrialRequest request) {
              return getBacktestTrial(jobId,trialId,request,null);
            }

            public ChronicleLabsApiHttpResponse<BacktestTrialDetailResponse> getBacktestTrial(
                String jobId, String trialId, GetBacktestTrialRequest request,
                RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("v1/backtests/jobs")
                .addPathSegment(jobId)
                .addPathSegments("trials")
                .addPathSegment(trialId);if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("GET", null)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new ChronicleLabsApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BacktestTrialDetailResponse.class), response);
                  }
                  try {
                    switch (response.code()) {
                      case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                      case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new ChronicleLabsApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (JsonProcessingException e) {
                  throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
                }
                catch (IOException e) {
                  throw new ChronicleLabsApiException("Network error executing HTTP request", e);
                }
              }

              public ChronicleLabsApiHttpResponse<CancelBacktestJobResponse> cancelBacktestJob(
                  String jobId) {
                return cancelBacktestJob(jobId,CancelBacktestJobRequest.builder().build());
              }

              public ChronicleLabsApiHttpResponse<CancelBacktestJobResponse> cancelBacktestJob(
                  String jobId, RequestOptions requestOptions) {
                return cancelBacktestJob(jobId,CancelBacktestJobRequest.builder().build(),requestOptions);
              }

              public ChronicleLabsApiHttpResponse<CancelBacktestJobResponse> cancelBacktestJob(
                  String jobId, CancelBacktestJobRequest request) {
                return cancelBacktestJob(jobId,request,null);
              }

              public ChronicleLabsApiHttpResponse<CancelBacktestJobResponse> cancelBacktestJob(
                  String jobId, CancelBacktestJobRequest request, RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("v1/backtests/jobs")
                  .addPathSegment(jobId)
                  .addPathSegments("cancel");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request.Builder _requestBuilder = new Request.Builder()
                    .url(httpUrl.build())
                    .method("POST", RequestBody.create("", null))
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json");
                  Request okhttpRequest = _requestBuilder.build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                    okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new ChronicleLabsApiHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CancelBacktestJobResponse.class), response);
                    }
                    try {
                      switch (response.code()) {
                        case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                        case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                        case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new ChronicleLabsApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (JsonProcessingException e) {
                    throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
                  }
                  catch (IOException e) {
                    throw new ChronicleLabsApiException("Network error executing HTTP request", e);
                  }
                }

                public ChronicleLabsApiHttpResponse<Iterable<TrialEvent>> streamBacktestJobEvents(
                    String jobId) {
                  return streamBacktestJobEvents(jobId,StreamBacktestJobEventsRequest.builder().build());
                }

                public ChronicleLabsApiHttpResponse<Iterable<TrialEvent>> streamBacktestJobEvents(
                    String jobId, RequestOptions requestOptions) {
                  return streamBacktestJobEvents(jobId,StreamBacktestJobEventsRequest.builder().build(),requestOptions);
                }

                public ChronicleLabsApiHttpResponse<Iterable<TrialEvent>> streamBacktestJobEvents(
                    String jobId, StreamBacktestJobEventsRequest request) {
                  return streamBacktestJobEvents(jobId,request,null);
                }

                public ChronicleLabsApiHttpResponse<Iterable<TrialEvent>> streamBacktestJobEvents(
                    String jobId, StreamBacktestJobEventsRequest request,
                    RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                    .addPathSegments("v1/backtests/jobs")
                    .addPathSegment(jobId)
                    .addPathSegments("stream");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    Request.Builder _requestBuilder = new Request.Builder()
                      .url(httpUrl.build())
                      .method("GET", null)
                      .headers(Headers.of(clientOptions.headers(requestOptions)));
                    Request okhttpRequest = _requestBuilder.build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                      okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                    }
                    client = client.newBuilder().callTimeout(0, TimeUnit.SECONDS).build();
                    try {
                      Response response = client.newCall(okhttpRequest).execute();
                      ResponseBody responseBody = response.body();
                      if (response.isSuccessful()) {
                        return new ChronicleLabsApiHttpResponse<>(Stream.fromSse(TrialEvent.class, new ResponseBodyReader(response)), response);
                      }
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      try {
                        switch (response.code()) {
                          case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                          case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                          case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorResponse.class), response);
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      throw new ChronicleLabsApiApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                    }
                    catch (JsonProcessingException e) {
                      throw new ChronicleLabsApiException("Failed to deserialize response: " + e.getMessage(), e);
                    }
                    catch (IOException e) {
                      throw new ChronicleLabsApiException("Network error executing HTTP request", e);
                    }
                  }
                }
