/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.chronicle.labs.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class TrialEvent {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private TrialEvent(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static TrialEvent jobStarted(TrialEventJobStarted value) {
        return new TrialEvent(new JobStartedValue(value));
    }

    public static TrialEvent trialPhaseChanged(TrialEventTrialPhaseChanged value) {
        return new TrialEvent(new TrialPhaseChangedValue(value));
    }

    public static TrialEvent trialRewardsRecorded(TrialEventTrialRewardsRecorded value) {
        return new TrialEvent(new TrialRewardsRecordedValue(value));
    }

    public static TrialEvent trialFinished(TrialEventTrialFinished value) {
        return new TrialEvent(new TrialFinishedValue(value));
    }

    public static TrialEvent jobFinished(TrialEventJobFinished value) {
        return new TrialEvent(new JobFinishedValue(value));
    }

    public boolean isJobStarted() {
        return value instanceof JobStartedValue;
    }

    public boolean isTrialPhaseChanged() {
        return value instanceof TrialPhaseChangedValue;
    }

    public boolean isTrialRewardsRecorded() {
        return value instanceof TrialRewardsRecordedValue;
    }

    public boolean isTrialFinished() {
        return value instanceof TrialFinishedValue;
    }

    public boolean isJobFinished() {
        return value instanceof JobFinishedValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<TrialEventJobStarted> getJobStarted() {
        if (isJobStarted()) {
            return Optional.of(((JobStartedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TrialEventTrialPhaseChanged> getTrialPhaseChanged() {
        if (isTrialPhaseChanged()) {
            return Optional.of(((TrialPhaseChangedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TrialEventTrialRewardsRecorded> getTrialRewardsRecorded() {
        if (isTrialRewardsRecorded()) {
            return Optional.of(((TrialRewardsRecordedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TrialEventTrialFinished> getTrialFinished() {
        if (isTrialFinished()) {
            return Optional.of(((TrialFinishedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TrialEventJobFinished> getJobFinished() {
        if (isJobFinished()) {
            return Optional.of(((JobFinishedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof TrialEvent && value.equals(((TrialEvent) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitJobStarted(TrialEventJobStarted jobStarted);

        T visitTrialPhaseChanged(TrialEventTrialPhaseChanged trialPhaseChanged);

        T visitTrialRewardsRecorded(TrialEventTrialRewardsRecorded trialRewardsRecorded);

        T visitTrialFinished(TrialEventTrialFinished trialFinished);

        T visitJobFinished(TrialEventJobFinished jobFinished);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "kind", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(JobStartedValue.class),
        @JsonSubTypes.Type(TrialPhaseChangedValue.class),
        @JsonSubTypes.Type(TrialRewardsRecordedValue.class),
        @JsonSubTypes.Type(TrialFinishedValue.class),
        @JsonSubTypes.Type(JobFinishedValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("job-started")
    @JsonIgnoreProperties("kind")
    private static final class JobStartedValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private TrialEventJobStarted value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private JobStartedValue() {}

        private JobStartedValue(TrialEventJobStarted value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitJobStarted(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof JobStartedValue && equalTo((JobStartedValue) other);
        }

        private boolean equalTo(JobStartedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TrialEvent{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("trial-phase-changed")
    @JsonIgnoreProperties("kind")
    private static final class TrialPhaseChangedValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private TrialEventTrialPhaseChanged value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TrialPhaseChangedValue() {}

        private TrialPhaseChangedValue(TrialEventTrialPhaseChanged value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTrialPhaseChanged(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TrialPhaseChangedValue && equalTo((TrialPhaseChangedValue) other);
        }

        private boolean equalTo(TrialPhaseChangedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TrialEvent{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("trial-rewards-recorded")
    @JsonIgnoreProperties("kind")
    private static final class TrialRewardsRecordedValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private TrialEventTrialRewardsRecorded value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TrialRewardsRecordedValue() {}

        private TrialRewardsRecordedValue(TrialEventTrialRewardsRecorded value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTrialRewardsRecorded(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TrialRewardsRecordedValue && equalTo((TrialRewardsRecordedValue) other);
        }

        private boolean equalTo(TrialRewardsRecordedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TrialEvent{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("trial-finished")
    @JsonIgnoreProperties("kind")
    private static final class TrialFinishedValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private TrialEventTrialFinished value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TrialFinishedValue() {}

        private TrialFinishedValue(TrialEventTrialFinished value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTrialFinished(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TrialFinishedValue && equalTo((TrialFinishedValue) other);
        }

        private boolean equalTo(TrialFinishedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TrialEvent{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("job-finished")
    @JsonIgnoreProperties("kind")
    private static final class JobFinishedValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "kind", allowSetters = true)
        private TrialEventJobFinished value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private JobFinishedValue() {}

        private JobFinishedValue(TrialEventJobFinished value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitJobFinished(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof JobFinishedValue && equalTo((JobFinishedValue) other);
        }

        private boolean equalTo(JobFinishedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TrialEvent{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("kind")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TrialEvent{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
