/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.chronicle.labs.api.types;

import com.chronicle.labs.api.core.Nullable;
import com.chronicle.labs.api.core.NullableNonemptyFilter;
import com.chronicle.labs.api.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = AddTaskFromTraceResponseMembership.Builder.class)
public final class AddTaskFromTraceResponseMembership {
    private final Optional<String> addedAt;

    private final String datasetId;

    private final String datasetName;

    private final int eventCount;

    private final String id;

    private final Optional<String> note;

    private final Optional<AddTaskFromTraceResponseMembershipPurpose> purpose;

    private final boolean refreshAvailable;

    private final int revision;

    private final Optional<AddTaskFromTraceResponseMembershipSplit> split;

    private final String subjectId;

    private final AddTaskFromTraceResponseMembershipSubjectKind subjectKind;

    private final String traceId;

    private final Map<String, Object> additionalProperties;

    private AddTaskFromTraceResponseMembership(
            Optional<String> addedAt,
            String datasetId,
            String datasetName,
            int eventCount,
            String id,
            Optional<String> note,
            Optional<AddTaskFromTraceResponseMembershipPurpose> purpose,
            boolean refreshAvailable,
            int revision,
            Optional<AddTaskFromTraceResponseMembershipSplit> split,
            String subjectId,
            AddTaskFromTraceResponseMembershipSubjectKind subjectKind,
            String traceId,
            Map<String, Object> additionalProperties) {
        this.addedAt = addedAt;
        this.datasetId = datasetId;
        this.datasetName = datasetName;
        this.eventCount = eventCount;
        this.id = id;
        this.note = note;
        this.purpose = purpose;
        this.refreshAvailable = refreshAvailable;
        this.revision = revision;
        this.split = split;
        this.subjectId = subjectId;
        this.subjectKind = subjectKind;
        this.traceId = traceId;
        this.additionalProperties = additionalProperties;
    }

    @JsonIgnore
    public Optional<String> getAddedAt() {
        if (addedAt == null) {
            return Optional.empty();
        }
        return addedAt;
    }

    @JsonProperty("datasetId")
    public String getDatasetId() {
        return datasetId;
    }

    @JsonProperty("datasetName")
    public String getDatasetName() {
        return datasetName;
    }

    @JsonProperty("eventCount")
    public int getEventCount() {
        return eventCount;
    }

    @JsonProperty("id")
    public String getId() {
        return id;
    }

    @JsonIgnore
    public Optional<String> getNote() {
        if (note == null) {
            return Optional.empty();
        }
        return note;
    }

    /**
     * @return Intended use of a dataset — drives the colored badge on the picker and lets apps route additions to the right backend (eval suite, training set, replay corpus, manual review queue).
     */
    @JsonIgnore
    public Optional<AddTaskFromTraceResponseMembershipPurpose> getPurpose() {
        if (purpose == null) {
            return Optional.empty();
        }
        return purpose;
    }

    @JsonProperty("refreshAvailable")
    public boolean getRefreshAvailable() {
        return refreshAvailable;
    }

    @JsonProperty("revision")
    public int getRevision() {
        return revision;
    }

    /**
     * @return Train / validation / test split assignment.
     */
    @JsonIgnore
    public Optional<AddTaskFromTraceResponseMembershipSplit> getSplit() {
        if (split == null) {
            return Optional.empty();
        }
        return split;
    }

    @JsonProperty("subjectId")
    public String getSubjectId() {
        return subjectId;
    }

    /**
     * @return The canonical event-store subject curated into a Dataset. Providers that cannot form a real trace keep a single event subject rather than inventing a trace identifier. <code>Task</code> marks a hand-authored task with no captured subject; its revision holds zero events and its subject id is minted by the service.
     */
    @JsonProperty("subjectKind")
    public AddTaskFromTraceResponseMembershipSubjectKind getSubjectKind() {
        return subjectKind;
    }

    /**
     * @return Compatibility identity consumed by the Timeline picker. For an event membership this is the event id.
     */
    @JsonProperty("traceId")
    public String getTraceId() {
        return traceId;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("addedAt")
    private Optional<String> _getAddedAt() {
        return addedAt;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("note")
    private Optional<String> _getNote() {
        return note;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("purpose")
    private Optional<AddTaskFromTraceResponseMembershipPurpose> _getPurpose() {
        return purpose;
    }

    @JsonInclude(value = JsonInclude.Include.CUSTOM, valueFilter = NullableNonemptyFilter.class)
    @JsonProperty("split")
    private Optional<AddTaskFromTraceResponseMembershipSplit> _getSplit() {
        return split;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof AddTaskFromTraceResponseMembership
                && equalTo((AddTaskFromTraceResponseMembership) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(AddTaskFromTraceResponseMembership other) {
        return addedAt.equals(other.addedAt)
                && datasetId.equals(other.datasetId)
                && datasetName.equals(other.datasetName)
                && eventCount == other.eventCount
                && id.equals(other.id)
                && note.equals(other.note)
                && purpose.equals(other.purpose)
                && refreshAvailable == other.refreshAvailable
                && revision == other.revision
                && split.equals(other.split)
                && subjectId.equals(other.subjectId)
                && subjectKind.equals(other.subjectKind)
                && traceId.equals(other.traceId);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(
                this.addedAt,
                this.datasetId,
                this.datasetName,
                this.eventCount,
                this.id,
                this.note,
                this.purpose,
                this.refreshAvailable,
                this.revision,
                this.split,
                this.subjectId,
                this.subjectKind,
                this.traceId);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static DatasetIdStage builder() {
        return new Builder();
    }

    public interface DatasetIdStage {
        DatasetNameStage datasetId(@NotNull String datasetId);

        Builder from(AddTaskFromTraceResponseMembership other);
    }

    public interface DatasetNameStage {
        EventCountStage datasetName(@NotNull String datasetName);
    }

    public interface EventCountStage {
        IdStage eventCount(int eventCount);
    }

    public interface IdStage {
        RefreshAvailableStage id(@NotNull String id);
    }

    public interface RefreshAvailableStage {
        RevisionStage refreshAvailable(boolean refreshAvailable);
    }

    public interface RevisionStage {
        SubjectIdStage revision(int revision);
    }

    public interface SubjectIdStage {
        SubjectKindStage subjectId(@NotNull String subjectId);
    }

    public interface SubjectKindStage {
        /**
         * <p>The canonical event-store subject curated into a Dataset. Providers that cannot form a real trace keep a single event subject rather than inventing a trace identifier. <code>Task</code> marks a hand-authored task with no captured subject; its revision holds zero events and its subject id is minted by the service.</p>
         */
        TraceIdStage subjectKind(@NotNull AddTaskFromTraceResponseMembershipSubjectKind subjectKind);
    }

    public interface TraceIdStage {
        /**
         * <p>Compatibility identity consumed by the Timeline picker. For an event membership this is the event id.</p>
         */
        _FinalStage traceId(@NotNull String traceId);
    }

    public interface _FinalStage {
        AddTaskFromTraceResponseMembership build();

        _FinalStage additionalProperty(String key, Object value);

        _FinalStage additionalProperties(Map<String, Object> additionalProperties);

        _FinalStage addedAt(Optional<String> addedAt);

        _FinalStage addedAt(String addedAt);

        _FinalStage addedAt(Nullable<String> addedAt);

        _FinalStage note(Optional<String> note);

        _FinalStage note(String note);

        _FinalStage note(Nullable<String> note);

        /**
         * <p>Intended use of a dataset — drives the colored badge on the picker and lets apps route additions to the right backend (eval suite, training set, replay corpus, manual review queue).</p>
         */
        _FinalStage purpose(Optional<AddTaskFromTraceResponseMembershipPurpose> purpose);

        _FinalStage purpose(AddTaskFromTraceResponseMembershipPurpose purpose);

        _FinalStage purpose(Nullable<AddTaskFromTraceResponseMembershipPurpose> purpose);

        /**
         * <p>Train / validation / test split assignment.</p>
         */
        _FinalStage split(Optional<AddTaskFromTraceResponseMembershipSplit> split);

        _FinalStage split(AddTaskFromTraceResponseMembershipSplit split);

        _FinalStage split(Nullable<AddTaskFromTraceResponseMembershipSplit> split);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements DatasetIdStage,
                    DatasetNameStage,
                    EventCountStage,
                    IdStage,
                    RefreshAvailableStage,
                    RevisionStage,
                    SubjectIdStage,
                    SubjectKindStage,
                    TraceIdStage,
                    _FinalStage {
        private String datasetId;

        private String datasetName;

        private int eventCount;

        private String id;

        private boolean refreshAvailable;

        private int revision;

        private String subjectId;

        private AddTaskFromTraceResponseMembershipSubjectKind subjectKind;

        private String traceId;

        private Optional<AddTaskFromTraceResponseMembershipSplit> split = Optional.empty();

        private Optional<AddTaskFromTraceResponseMembershipPurpose> purpose = Optional.empty();

        private Optional<String> note = Optional.empty();

        private Optional<String> addedAt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(AddTaskFromTraceResponseMembership other) {
            addedAt(other.getAddedAt());
            datasetId(other.getDatasetId());
            datasetName(other.getDatasetName());
            eventCount(other.getEventCount());
            id(other.getId());
            note(other.getNote());
            purpose(other.getPurpose());
            refreshAvailable(other.getRefreshAvailable());
            revision(other.getRevision());
            split(other.getSplit());
            subjectId(other.getSubjectId());
            subjectKind(other.getSubjectKind());
            traceId(other.getTraceId());
            return this;
        }

        @java.lang.Override
        @JsonSetter("datasetId")
        public DatasetNameStage datasetId(@NotNull String datasetId) {
            this.datasetId = Objects.requireNonNull(datasetId, "datasetId must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("datasetName")
        public EventCountStage datasetName(@NotNull String datasetName) {
            this.datasetName = Objects.requireNonNull(datasetName, "datasetName must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("eventCount")
        public IdStage eventCount(int eventCount) {
            this.eventCount = eventCount;
            return this;
        }

        @java.lang.Override
        @JsonSetter("id")
        public RefreshAvailableStage id(@NotNull String id) {
            this.id = Objects.requireNonNull(id, "id must not be null");
            return this;
        }

        @java.lang.Override
        @JsonSetter("refreshAvailable")
        public RevisionStage refreshAvailable(boolean refreshAvailable) {
            this.refreshAvailable = refreshAvailable;
            return this;
        }

        @java.lang.Override
        @JsonSetter("revision")
        public SubjectIdStage revision(int revision) {
            this.revision = revision;
            return this;
        }

        @java.lang.Override
        @JsonSetter("subjectId")
        public SubjectKindStage subjectId(@NotNull String subjectId) {
            this.subjectId = Objects.requireNonNull(subjectId, "subjectId must not be null");
            return this;
        }

        /**
         * <p>The canonical event-store subject curated into a Dataset. Providers that cannot form a real trace keep a single event subject rather than inventing a trace identifier. <code>Task</code> marks a hand-authored task with no captured subject; its revision holds zero events and its subject id is minted by the service.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("subjectKind")
        public TraceIdStage subjectKind(@NotNull AddTaskFromTraceResponseMembershipSubjectKind subjectKind) {
            this.subjectKind = Objects.requireNonNull(subjectKind, "subjectKind must not be null");
            return this;
        }

        /**
         * <p>Compatibility identity consumed by the Timeline picker. For an event membership this is the event id.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("traceId")
        public _FinalStage traceId(@NotNull String traceId) {
            this.traceId = Objects.requireNonNull(traceId, "traceId must not be null");
            return this;
        }

        /**
         * <p>Train / validation / test split assignment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage split(Nullable<AddTaskFromTraceResponseMembershipSplit> split) {
            if (split.isNull()) {
                this.split = null;
            } else if (split.isEmpty()) {
                this.split = Optional.empty();
            } else {
                this.split = Optional.of(split.get());
            }
            return this;
        }

        /**
         * <p>Train / validation / test split assignment.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage split(AddTaskFromTraceResponseMembershipSplit split) {
            this.split = Optional.ofNullable(split);
            return this;
        }

        /**
         * <p>Train / validation / test split assignment.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "split", nulls = Nulls.SKIP)
        public _FinalStage split(Optional<AddTaskFromTraceResponseMembershipSplit> split) {
            this.split = split;
            return this;
        }

        /**
         * <p>Intended use of a dataset — drives the colored badge on the picker and lets apps route additions to the right backend (eval suite, training set, replay corpus, manual review queue).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage purpose(Nullable<AddTaskFromTraceResponseMembershipPurpose> purpose) {
            if (purpose.isNull()) {
                this.purpose = null;
            } else if (purpose.isEmpty()) {
                this.purpose = Optional.empty();
            } else {
                this.purpose = Optional.of(purpose.get());
            }
            return this;
        }

        /**
         * <p>Intended use of a dataset — drives the colored badge on the picker and lets apps route additions to the right backend (eval suite, training set, replay corpus, manual review queue).</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage purpose(AddTaskFromTraceResponseMembershipPurpose purpose) {
            this.purpose = Optional.ofNullable(purpose);
            return this;
        }

        /**
         * <p>Intended use of a dataset — drives the colored badge on the picker and lets apps route additions to the right backend (eval suite, training set, replay corpus, manual review queue).</p>
         */
        @java.lang.Override
        @JsonSetter(value = "purpose", nulls = Nulls.SKIP)
        public _FinalStage purpose(Optional<AddTaskFromTraceResponseMembershipPurpose> purpose) {
            this.purpose = purpose;
            return this;
        }

        @java.lang.Override
        public _FinalStage note(Nullable<String> note) {
            if (note.isNull()) {
                this.note = null;
            } else if (note.isEmpty()) {
                this.note = Optional.empty();
            } else {
                this.note = Optional.of(note.get());
            }
            return this;
        }

        @java.lang.Override
        public _FinalStage note(String note) {
            this.note = Optional.ofNullable(note);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "note", nulls = Nulls.SKIP)
        public _FinalStage note(Optional<String> note) {
            this.note = note;
            return this;
        }

        @java.lang.Override
        public _FinalStage addedAt(Nullable<String> addedAt) {
            if (addedAt.isNull()) {
                this.addedAt = null;
            } else if (addedAt.isEmpty()) {
                this.addedAt = Optional.empty();
            } else {
                this.addedAt = Optional.of(addedAt.get());
            }
            return this;
        }

        @java.lang.Override
        public _FinalStage addedAt(String addedAt) {
            this.addedAt = Optional.ofNullable(addedAt);
            return this;
        }

        @java.lang.Override
        @JsonSetter(value = "addedAt", nulls = Nulls.SKIP)
        public _FinalStage addedAt(Optional<String> addedAt) {
            this.addedAt = addedAt;
            return this;
        }

        @java.lang.Override
        public AddTaskFromTraceResponseMembership build() {
            return new AddTaskFromTraceResponseMembership(
                    addedAt,
                    datasetId,
                    datasetName,
                    eventCount,
                    id,
                    note,
                    purpose,
                    refreshAvailable,
                    revision,
                    split,
                    subjectId,
                    subjectKind,
                    traceId,
                    additionalProperties);
        }

        @java.lang.Override
        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        @java.lang.Override
        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
